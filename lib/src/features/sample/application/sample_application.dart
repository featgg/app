/// Application layer of sample.
///
/// Optional orchestration/use-case layer that coordinates domain objects and
/// emits state consumed by presentation. Depends on domain only.
/// Placeholder barrel — no public API yet.
library;
