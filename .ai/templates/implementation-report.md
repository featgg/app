# Implementation report: <task>

## Done

- ...

## Deviations from plan

- <what changed and why; "none" if clean. Each deviation is a flag for the reviewer to re-verify against canon.>

## Tests added

testing_policy: <none | existing | required>

- <new tests with paths and what each enforces; "none" if testing_policy was none or existing>

## Verification

<one bullet per Stage-2 command from the plan's "Verification commands": exit status plus the relevant counts (tests passed/failed, analyze issues). Paste an output excerpt only for a red command. Missing or red is a hard stop.>

- `<command>`: <green (counts) | red, with failing excerpt>

## Doc updates applied

<echo the plan's "Doc updates"; mark each as applied / deviated / not-applicable. If the plan declared "no doc changes", write that here verbatim.>

- `docs/<file>.md` § <Section> — applied as planned / deviated: <what>

## Pending

- <out-of-scope follow-ups, manual steps not yet performed, environment-blocked checks, defects surfaced for the planner to re-scope; "none" if clean.>

---

— app-implementer · next: app-reviewer
